import { PdfTextEditDirective } from './pdf-text-edit.directive';

describe('PdfTextEditDirective', () => {
  it('should create an instance', () => {
    const directive = new PdfTextEditDirective();
    expect(directive).toBeTruthy();
  });
});
