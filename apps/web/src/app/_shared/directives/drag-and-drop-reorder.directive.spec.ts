import { DragAndDropReorderDirective } from './drag-and-drop-reorder.directive';

describe('DragAndDropReorderDirective', () => {
  it('should create an instance', () => {
    const directive = new DragAndDropReorderDirective();
    expect(directive).toBeTruthy();
  });
});
