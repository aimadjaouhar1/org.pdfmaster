version: '3'

services:   
  web:
    container_name: web
    image: aimadjaouhar/org.pdfmaster:web-0.1.5
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/app.conf:/etc/nginx/conf.d/default.conf:ro